{
    "name": "testapp",
    "version": "0.1.0",
    "private": true,
    "optionalDependencies": { "fsevents": "2.x.x" },
    "engines": {
        "node":"14.x",
        "npm":"7.10.0"
    },
    "dependencies": {

    },
    "scripts": {
        "postinstall": "cd frontend && npm install --only=dev && NODE_ENV=development npm install --no-shrinkwrap",
        "start": "cd frontend && npm run start",
        "build": "cd frontend && npm run build",
        "test": "cd frontend && npm run test",
        "eject": "cd frontend && npm run eject"
    }
}